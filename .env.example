# Claude API Key (required)
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: MCP Server Configurations
# MCP_FILESYSTEM_PATH=/custom/path

# OAuth Connectors (optional)
# Configure OAuth credentials to enable connectors for Gmail, Drive, Slack, Notion, GitHub

# Google OAuth (Gmail + Drive)
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Required scopes: gmail.readonly, drive.readonly, userinfo.email, userinfo.profile
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Slack OAuth
# Create app at: https://api.slack.com/apps
# Required scopes: channels:read, channels:history, chat:write, users:read
# SLACK_CLIENT_ID=your-slack-client-id
# SLACK_CLIENT_SECRET=your-slack-client-secret

# Notion OAuth
# Create integration at: https://www.notion.so/my-integrations
# Access is determined by pages shared with the integration
# NOTION_CLIENT_ID=your-notion-client-id
# NOTION_CLIENT_SECRET=your-notion-client-secret

# GitHub OAuth
# Create app at: https://github.com/settings/developers
# Required scopes: public_repo, read:user
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret
